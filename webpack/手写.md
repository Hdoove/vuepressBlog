### 手写

```javascript
/**
 * loader 其实就是一个方法
 */

module.export = function (context) {
    // 比如我们对文件中的console.log进行删除
    var context = context.replace(/console.log\('?\w*'?\)/g, '');
    //然后再返回出去
    return context;
}

/**
 *plugin
 */

a.prototype.apply = function(compiller) {
    // 插件原理  监听打包过程中的某一个生命周期
    // 在webpack done这个生命周期下注册一个插件 插件的名字是changeStatic，它所执行的方法就是后面的回调函数
    compiller.hooks.done.tap('changeStatic', function(compilation) {
        let context = compiller.options.context;
        compilation.toJson().assets.forEach(item => {
            
        });
    });
}
```